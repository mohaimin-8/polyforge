# Wave 3 live chaos + p99 — results against the frozen readings

Generated by `live_chaos_p99.py` from the committed run-level record
`eval/results/live_chaos_p99_runs.csv` (session 23 live sitting, commit
7bdbd4f). Protocol: `PREREG_LIVE_CHAOS_P99.md`, pushed 2026-07-16 before
any live number existed. This file adds no new measurement.

## Substrate, as run (disclosures)

- jcac arm only, one execution per cell — a demonstration that the
  designed fallback works over the wire, not a powered comparison
  (scoped so in the prereg).
- **Shared-PG data plane:** the session-23 sitting ran the control plane
  against a shared PostgreSQL data plane rather than the session-19
  per-pod fixture — the substrate deviation that made the Codespace run
  fit its quota. Fault injections, offsets, and cell geometry are the
  frozen ones.
- The `ai_cacheable` row is the fault-free smoke (Part B p99 source for
  that cell); both chaos faults ran on the `crud_bursty` cell, per row.

## Run table (committed record, verbatim)

| cell | steps | faults | mean_violation | jain | cost $ | crud p95/p99 ms | ai p95/p99 ms |
|---|---|---|---|---|---|---|---|
| ai_cacheable | 30 | none | 0.0000 | 1.0000 | 1.6678 | 1.0084 / 1.1911 | 20.0077 / 20.0431 |
| crud_bursty | 135 | planner-crash@T8min+apf-throttle@T14min | 0.0000 | 1.0000 | 0.2880 | 2.9827 / 8.0072 | 0.0000 / 0.0000 |

## Part A — LC-H1 (planner crash + apiserver throttle)

- Cell `crud_bursty`: planner-crash@T8min+apf-throttle@T14min over 135 steps
  (~22.5 min at 10 s/step).
- Run-level mean_violation is **0.0000**. Violation is non-negative, so a zero mean over all 135 steps implies **every step is zero** — including the planner-crash window at T=8 min and the 60 s APF throttle at T=14 min. There was no violation excursion to recover from: the last-known-good plan hold carried both faults at zero violation under the cell's burst load.
- **LC-H1 PASS** (strongest form): the post-fault level equals the pre-fault level exactly, trivially within CHAOS_TOL = 0.05 and within the 5-minute window. The recovery-curve reading is degenerate by absence of any excursion; the per-step series in the archived duckdb is the supporting record.

## Part B — P99-H1 (live p95 and p99 side by side; live-only numbers)

| cell | family | p95 ms | p99 ms | p99/p95 |
|---|---|---|---|---|
| ai_cacheable | crud | 1.008 | 1.191 | 1.181 |
| ai_cacheable | ai | 20.008 | 20.043 | 1.002 |
| crud_bursty | crud | 2.983 | 8.007 | 2.685 |
| crud_bursty | ai | — | — | (no ai traffic in cell) |

- Violation is zero in every committed row, so the **p99 violation verdict matches the p95 verdict** in both cells: on the live cluster the SLO story is unchanged when read at p99 — exactly the narrow claim P99-H1 permits.
- The largest tail spread is the bursty CRUD family (p99/p95 ≈ 2.68 in `crud_bursty`); the AI family under the mock backend is nearly flat (≈ 1.00). Per ground rule 5 these are **live-only** readings — no sim table is revisited with them.

## Status

- DEFENSE_QA #19 (live-chaos half): closed by Part A — both frozen
  faults injected live under load, run valid, zero violation through
  both.
- DEFENSE_QA #11 (p99 half): closed by Part B — first real live p99
  numbers recorded, SLO verdict unchanged at p99.
- Stopping rule honored: one live sitting, no widening; this document
  is generated once from the committed record.
