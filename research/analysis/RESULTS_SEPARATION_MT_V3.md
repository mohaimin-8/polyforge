# The eight-tenant coupled floor — computed, and falsified (WP13 step 3)

Generated by `analysis_separation_mt_v3.py` from `research/jcac_sim/guarantee.py` and the committed walk artifact `separation_mt_v3_walk.json`. Every number is computed at run time or read from that artifact; nothing is transcribed.

**This record replaces no earlier one.** `RESULTS_SEPARATION_MT.md` (step 1, falsified) and `RESULTS_SEPARATION_MT_V2.md` (step 2, S3 not evaluable) stand exactly as committed. This is the step v2 named as next: the walk it could not afford.


## Headline

The eight-tenant floor **is computable**: **0.126189**, from an exact ordered walk over **268,435,456 offset vectors** in **119.9 minutes**. v2 reported this same quantity as NOT EVALUABLE; the obstacle was the enumeration loop, not the problem.

**S3 FAIL.** **2 arm(s) measure strictly below it**, so the corrected multi-tenant derivation is FALSIFIED — for the second time, now with the number in hand rather than out of reach. The separation theorem stays scoped to a single tenant **because the multi-tenant extension was tested and failed**, which is a different and stronger statement than v2's.


## S5 — is the committed walk current?

The expensive rows cannot live inside `scripts/reproduce.py`: the largest walk in the artifact takes ~120 minutes and the gate re-runs every analysis. So they are computed once into `separation_mt_v3_walk.json` and the cheap rows are **re-derived here at gate time** and compared. A stale or edited artifact fails this check instead of passing quietly.

The comparison is to a relative tolerance of 1e-12, not exact: `coupled_floor_incremental_batched` documents its aggregate as a **chunked** float64 sum whose last bits depend on how the reduction is split, so an exact test measured the platform rather than the artifact. It did — on Linux row 6 re-derives as 0.04905754327774048 against the committed 0.0490575432777405, one ulp on a number reported to six decimals. The tolerance is six orders tighter than that reported precision, and a stale artifact misses by orders of magnitude.

Rows re-derived: **1..6**. **S5 PASS**.


## S1 — soundness

| degenerate case | floor | published | identical? |
|---|---:|---:|---|
| one tenant | 0.000000 | 0.000000 | yes |
| three tenants, cap at their maximum hold | 0.000000 | 0.000000 | yes |

**S1 PASS** — computed through the batched walk, so this also re-checks that vectorising did not change the model.


## S2 — the multiset shortcut is still forbidden

Re-run rather than cited. This record does **not** use the multiset enumeration; it walks every ordered vector. S2 is reported because a derivation that quietly assumed invariance would be wrong in exactly the way v2 caught.

Largest spread across permutations of 6 sampled offset multisets at 8 tenants: **0.171875** (v2 measured 0.171875).

**S2 FAIL** — sweep order **changes** the mean per-tenant violation. The ordered walk is unaffected either way; it is the cheap shortcut that this forbids.


## S3 — does the floor bound the measured arms?

Floor at 8 tenants: **0.126189**


| arm | measured mean_violation | at or above the floor? |
|---|---:|---|
| `keda` | 0.001886 | **NO** |
| `hpa` | 0.126042 | **NO** |
| `jcac_seasonal` | 0.244375 | yes |
| `jcac` | 0.265417 | yes |
| `firm` | 0.265761 | yes |

**S3 FAIL.** The binding arm is `keda` at 0.001886, 67x below the derived floor. Per the reading stated before the walk finished, this is reported and not repaired.


### How far below, per arm (descriptive — does not gate)

| arm | measured | shortfall vs floor | reading |
|---|---:|---:|---|
| `keda` | 0.001886 | 98.51% (66.9x) | the floor does not describe this arm |
| `hpa` | 0.126042 | 0.12% (1.0x) | within 1% of the floor — a calibration miss |

So the failure is not uniform: `hpa` sits essentially ON the floor, while `keda` sits orders of magnitude under it. A floor that a reactive replica autoscaler meets to within a fraction of a percent and an event-driven one misses by two orders of magnitude is not merely mis-scaled — it is pricing a contention the event-driven arm does not pay. Naming that mechanism is future work and is NOT attempted here; this record reports the falsification.


## S4 — the exact series (descriptive)

Every row is the ordered enumeration, exact, with no shortcut.

| tenants | offset vectors | cap can bind? | floor | walk seconds |
|---:|---:|---|---:|---:|
| 1 | 1 | no | 0.000000 | 0.0 |
| 2 | 16 | no | 0.000000 | 0.0 |
| 3 | 256 | no | 0.000000 | 0.0 |
| 4 | 4,096 | no | 0.000000 | 0.0 |
| 5 | 65,536 | yes | 0.016514 | 0.7 |
| 6 | 1,048,576 | yes | 0.049058 | 19.0 |
| 7 | 16,777,216 | yes | 0.090102 | 391.5 |
| 8 | 268,435,456 | yes | 0.126189 | 7191.3 |

The floor is zero until the cap can bind at 6-replica ceilings against a cap of 24, then rises monotonically. Step 1's falsified floor was 0.036487; the corrected derivation crosses that between six and seven tenants, so the correction made the floor HIGHER at the published cell, not lower.


## What this changes for the thesis

v2's limitation was *"the eight-tenant floor is not computed"*. That sentence is now wrong and must not be repeated. The limitation is *"the multi-tenant floor was computed and does not bound the measured arms, so the separation result is stated for a single tenant"*. The theorem's scope is unchanged; the reason for it is now a measurement rather than a budget.

