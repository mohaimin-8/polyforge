# Pareto dominance — a weight-free reading of the headline matrix

Generated by `analysis_dominance.py` from the committed `eval/results/metrics_full.csv.gz`. Every number is computed at run time.

An arm is **dominated** in a cell when another arm is no worse on **every** raw objective (`total_cost_usd`, `mean_violation`, `1 - mean_jain`) and strictly better on at least one. A non-dominated arm is one that no other arm matches or beats on every objective at once. It is not the winner under every weighting: a baseline that is better on one objective and worse on another still wins when that objective is weighted heavily. What non-dominance does say is that no baseline beats it without a trade-off.

> Descriptive, not a hypothesis test — but note that dominance has **no free parameters**: no weights, no thresholds, no feature selection. There is nothing here to tune toward a preferred answer.


## D1 — is this the headline matrix?

| quantity | expected | found | agrees? |
|---|---:|---:|---|
| runs | 1800 | 1800 | yes |
| arms | 6 | 6 | yes |
| cells | 60 | 60 | yes |

**D1 PASS.**

## D2 — non-dominated cells per arm

| arm | non-dominated cells (of 60) | share |
|---|---:|---:|
| **`jcac`** | **48** | **80.0%** |
| `gptcache` | 32 | 53.3% |
| `keda` | 30 | 50.0% |
| `static` | 30 | 50.0% |
| `hpa` | 19 | 31.7% |
| `firm` | 3 | 5.0% |

jcac is non-dominated in **48 of 60** cells, the highest of any arm. In those cells every baseline that is better on one objective is worse on another. These counts are taken on 5-rep cell means; D5 below says how many of them survive resampling the reps.


## D3 — where jcac IS dominated

**12 cells**, listed in full because a dominance result that reported only its wins would be worthless.


| workload | tenant mix | cluster | dominated by |
|---|---|---|---|
| `crud_steady` | besteffort_heavy | large | `gptcache,hpa` |
| `crud_steady` | besteffort_heavy | medium | `gptcache,hpa` |
| `crud_steady` | besteffort_heavy | small | `gptcache,hpa` |
| `crud_steady` | premium_heavy | large | `gptcache,hpa` |
| `crud_steady` | premium_heavy | medium | `gptcache,hpa` |
| `crud_steady` | premium_heavy | small | `gptcache,hpa` |
| `crud_steady` | uniform | large | `gptcache,hpa` |
| `crud_steady` | uniform | medium | `gptcache,hpa` |
| `crud_steady` | uniform | small | `gptcache,hpa` |
| `crud_steady` | whale | large | `gptcache,hpa` |
| `crud_steady` | whale | medium | `gptcache,hpa` |
| `crud_steady` | whale | small | `gptcache,hpa` |


## D4 — specificity

| workload | AI rps | shape | jcac dominated |
|---|---:|---|---:|
| `agentic` | 2.7 | bursty | 0/12 |
| `ai_cacheable` | 7.0 | wave | 0/12 |
| `ai_uncacheable` | 3.2 | sawtooth | 0/12 |
| `crud_bursty` | 0.0 | bursty | 0/12 |
| `crud_steady` | 0.0 | steady | 12/12 |

Every exception is `crud_steady`, and it is dominated by the same pair in all of them. The obvious explanation is that it carries no AI traffic, so the cache and tier knobs have nothing to act on and jcac pays for machinery it cannot use.

**That explanation is incomplete, and the control says so:** `crud_bursty` is equally CRUD-only and jcac is **not** dominated there. What separates them is shape — `crud_steady` is `steady` while the control is `bursty`. The loss is specific to demand that is BOTH free of AI and free of variation: the one regime where neither the AI knobs nor the forecaster has anything to do, so the joint controller is pure overhead. Where either has something to act on, it is non-dominated.


## D5 — how stable is the count? (rep bootstrap)

Each arm's 5 reps are resampled with replacement 2,000 times per cell (seed 20260926) and dominance is re-judged on each resample. A cell is **stably non-dominated** when jcac is non-dominated in at least 95% of resamples, **stably dominated** when in at most 5%, and **unstable** otherwise.

| reading | cells |
|---|---:|
| stably non-dominated | 48 |
| unstable | 0 |
| stably dominated | 12 |

Of the 48 cells D2 counts as non-dominated, the rep noise supports 48 as stable, and no cell's dominance status depends on which reps were drawn. This bounds rep noise only: the cells are still simulator cells, scored against the originally tuned baselines.

