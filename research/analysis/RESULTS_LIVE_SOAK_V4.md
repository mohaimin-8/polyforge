# Live CRUD-plane soak — attempt 6: INVALID on a 19.5-second stall, and the first sitting to produce scored results (WP14)

Generated by `analysis_live_soak_v4.py` from evidence committed under `eval/results/live_soak_attempt6_evidence/`. Pre-registration: `PREREG_LIVE_SOAK_V4.md`, committed and pushed before the run.


## Headline — SK-H4 FAILS, and the cause is now isolated

The sitting delivered **11,529,271 requests** over **10 h 44 m** at **0.0018% failed** with **0 pod restarts**, and was ended by **10 dropped iterations**.

| gate | observed | threshold | verdict |
|---|---:|---:|---|
| `http_req_failed` | 0.0018% | < 1% | PASS |
| `dropped_iterations` | **10** | < 1 | **FAIL** |

**The load generator was not the limiting factor.** It held **9,601 virtual users** and peaked at **4,876** — it never came close to exhaustion. The worst request took **19.5 s**. A pool sized to absorb that at the cell's 94 req/s peak would need ~1834 VUs per tenant; this one covers 12.8 s.

`PREREG_LIVE_SOAK_V4` named this before the run: *"If a stall longer than 12.8 s occurs, SK-H4 fails again and that is the honest outcome … the generator is no longer the limiting factor and the stall is."* **The threshold is not relaxed**, as committed since V2.


## What the run achieved before it stopped

| quantity | value |
|---|---:|
| requests delivered | 11,529,271 |
| load duration | 38,647 s (10.74 h) |
| failed requests | 0.0018% |
| pod restarts | **0** |
| peak node memory | 22% |
| events recorded | 11,529,440 |
| whole-run `mean_violation` | 1.65e-06 |
| whole-run Jain fairness | 1.000000 |
| client p95 / max | 51.48 ms / 19515 ms |

For contrast, attempt 4 lost **4.567%** of its requests to a pinned load path and recorded no metrics at all. This run lost 0.0018% and recorded eleven and a half million events.

**The rebuilt exporter proved itself here.** It reduced 11,529,440 events in 78 seconds. The in-memory path it replaced OOM-killed the control-plane pod at ~952k events, which is why four earlier sittings recorded nothing.


## Hypotheses — the first scored live result in this work package

| hypothesis | verdict |
|---|---|
| SK-H1 recovery within 5 min, 20% band | **FAIL** |
| SK-H2 audit continuity | **PASS** |
| SK-H3 hourly `crud_p95` <= 8.0072 ms | **PASS** |
| SK-H4 validity | **FAIL** (10 dropped) |
| SK-H6 load distribution | **UNSCOREABLE** — the sampler summary is not preserved on an aborted run |
| SK-H7 client latency (descriptive) | p95 51.48 ms, median 8.12 ms, max 19515 ms |

### SK-H1 — recovery, per occurrence

Scored on the 60 s buckets, comparing the worst minute in the five minutes AFTER each injection ends against the median of the ten minutes before it began.

| fault | pre-fault median | worst during | worst in recovery | deviation | verdict |
|---|---:|---:|---:|---:|---|
| planner #1 | 8.005 ms | 8.010 ms | 8.009 ms | +0.1% | PASS |
| cpu #1 | 6.712 ms | 8.006 ms | 8.004 ms | +19.3% | PASS |
| planner #2 | 4.755 ms | 4.889 ms | 7.071 ms | +48.7% | **FAIL** |
| cpu #2 | — | — | — | — | not scoreable — fired after the load window ended |
| planner #3 | — | — | — | — | not scoreable — fired after the load window ended |

The rule passes only if every occurrence passes, so **SK-H1 FAILS**. The interesting detail is *where*: on `planner #2` the worst minute during the outage was barely above baseline, and the excursion arrived AFTER the planner returned. That is consistent with the planner re-planning every tenant at once on recovery rather than with the outage itself hurting, and it is a controller behaviour worth naming rather than an instrument artefact.

### SK-H2 — audit continuity

| injection | healthy window | degraded window | floor | verdict |
|---|---:|---:|---:|---|
| T+120m planner | +72 | +72 | 64 | PASS |
| T+480m planner | +72 | +72 | 64 | PASS |
| T+840m planner | — | — | — | excluded — fired after the load window ended |

Both planner outages produced **identical** windows six hours apart — 72 records healthy, 72 degraded, which is 9 control cycles x 8 tenants each time. The audit backbone does not slow down when the controller degrades. This is the third formulation of SK-H2 and the first to be scored on a sitting.

### SK-H3 — latency stability, and an honest caveat about its margin

All **12** hour-buckets sit at or below the 8.0072 ms threshold, so SK-H3 **PASSES**.

| hour | crud_p95 (ms) |
|---:|---:|
| 0 | 5.353 |
| 1 | 4.759 |
| 2 | 8.004 |
| 3 | 4.039 |
| 4 | 4.768 |
| 5 | 6.075 |
| 6 | 6.445 |
| 7 | 4.989 |
| 8 | 4.805 |
| 9 | 4.649 |
| 10 | 8.004 |
| 11 | 8.003 |

**The margin is 0.003 ms and that is not a rounding detail.** The largest hour-bucket is 8.004 ms against a threshold of 8.0072. Across the 60 s buckets, more than a hundred land inside 8.001–8.009 ms: `latency_ms` is a `burnCPU` duration quantised by work units, so p95 settles ON a discrete tier rather than near one — and the threshold, inherited from the committed B2 `crud_p99`, sits in the same tier. SK-H3 passes as written. A reader should not take that pass as evidence of comfortable headroom, because there is none.


## What is established, and what is not

**Established.** This system ran **10 h 44 m** of continuous fault-injected load at 0.0018% failure with **zero pod restarts** and flat memory, and produced scored results against pre-registered hypotheses. That is a categorically stronger live result than anything earlier in this work package, where the previous best with working instruments was four hours and no sitting had ever been scoreable.

**Established.** The failure causes have moved every attempt and are now exhausted one by one: not the load path (NodePort, gated), not the exporter (11.5M events in 78 s), not the instruments (three of four scored), not the generator (4,876 VUs used of 9,601), not memory (zero restarts), and not the controller.

**NOT established.** A complete 24 h run. The remaining obstacle is a stall of **19.5 s** against an absolute zero-drop gate. Its cause is unidentified; every observation points at host storage — `pgdata` is an `emptyDir` on a VHDX under WSL2 and the control plane writes telemetry synchronously inside the request handler — but that remains a hypothesis, not a result.

**NOT established.** That SK-H1 holds. It failed on one of three scoreable occurrences, and the failure is on the recovery side rather than under the fault.


## Disposition

Per the V4 stopping rule, the next step is **different hardware, not a seventh sitting on this one**. The stall exceeds what any VU pool can absorb, and no change available on this machine addresses it.

What a completed sitting needs:

1. **Storage that does not stall for 19 seconds** — a real block device rather than an `emptyDir` over a VHDX. A modest cloud VM would settle it in one day.
2. **Or a telemetry write path that does not block the handler.** That is a product change and needs its own pre-registration, because it alters what the live plane measures.

`RESULTS_LIVE_SOAK_V3.md` and `RESULTS_LIVE_SOAK_V2.md` stand as committed. This record does not replace them and does not soften them.

