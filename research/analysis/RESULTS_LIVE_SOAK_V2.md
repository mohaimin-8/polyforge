# Live CRUD-plane soak — attempt 4: the run is INVALID (WP14)

Generated by `analysis_live_soak_v2.py` from evidence committed under `eval/results/live_soak_evidence/`. Pre-registration: `PREREG_LIVE_SOAK_V2.md`, committed and pushed before the run.


## Headline — SK-H4 FAILS, for the third time in four sittings

The soak ran its full **24 h** (86,705 s wall), injected all 8 frozen faults, and was rejected by `cluster_backend.check_k6_delivery`:


| gate | observed | threshold | verdict |
|---|---:|---:|---|
| `http_req_failed` | **4.5674%** (1,167,884 of 25,569,795) | < 1% | **FAIL** |
| `dropped_iterations` | **236,566** | < 1 | **FAIL** |

| runner verdict | value |
|---|---:|
| `expected_runs` | 1 |
| `valid_runs` | 0 |
| `failed_runs` | 1 |
| `null_metrics` | 0 |
| `ok` | **false** |

No `eval-export.json` was preserved, so the run recorded **no metrics rows**. As in attempt 2, this is not a weak result but an absent one.


## The mechanism, which attempt 4 established

Earlier sittings recorded *that* delivery failed. This one recorded *why*, and the two vantage points on the same traffic make it unmistakable:


| measured at | p95 | includes |
|---|---:|---|
| server (Postgres `telemetry_events`) | **2.23 ms** | control-plane handling only |
| client (k6 `http_req_duration`) | **1032 ms** | + `kubectl port-forward` |

A **464x** gap. The control plane served requests in ~2.2 ms throughout; the load path in front of it reached 62 s at worst.

`kubectl port-forward service/…` resolves to **one pod** when it starts and never follows the Service. All ~300 req/s therefore landed on a single replica out of sixteen, which climbed to its 256 Mi limit, was OOM-killed, took the forward down with it, and was replaced by a fresh pod that repeated the cycle.


**282 forward restarts** over the run — 150 after health probe failed, 132 after process exited — against 286 health-probe failures.


A sixteen-replica deployment behind a port-forward has the fault tolerance of **one replica**, and no health signal reports it: pods stay `Running`, the Service keeps sixteen endpoints, and `/healthz` answers 200 between kills. The 5-minute delivery probe called the system healthy for 100 minutes while pods were dying every 2 minutes.

**This is a property of the evaluation harness's load path, not of the controller.** It says nothing about PolyForge behind a Service or Ingress that balances across endpoints, and "PolyForge OOMs under sustained load" would be the wrong conclusion to draw from it.


## Hypotheses


| id | verdict | basis |
|---|---|---|
| **SK-H4** validity | **FAIL** | 4.567% failed, 236,566 dropped |
| **SK-H3** latency | **PASS** | 17 hour buckets, 0 above 8.0072 ms |
| **SK-H1** recovery | **VACUOUS** | `mean_violation` spans 0.00e+00–4.22e-06; a 375 ms target against an ~8 ms p99 leaves the ±0.05 rule unable to fail. Its recovery path never ran: zero `fallback()` lines across seven planner kills. |
| **SK-H2** audit | **VACUOUS** | no audit stream exists; `PlanRunner.audit` no-ops when `Audit` is nil and `POLYFORGE_NATS_URL` is set nowhere. 0 == 0. |
| **SK-H5** forward restarts | descriptive | 282 restarts — **the OOM cycle, not supervisor merit** |

### Two vacuous passes are failures of the experiment, not successes of the system

A hypothesis that cannot fail has not been passed; it has not been run. SK-H1 and SK-H2 would have computed the same passing value in attempts 1, 2 and 3, and would again on a flawless attempt 5, because neither depends on the controller behaving well. Recording them as "PASS" would put two green cells in a results table that mean precisely nothing.

**SK-H5 is contaminated and must not be read as a result.** It was registered as descriptive, and until T+17.5 h its answer was *zero* restarts — which the prereg had already fixed as meaning the attempt-2 port-forward diagnosis stays unconfirmed. The final count describes the OOM cycle destroying the forward's target, and in part an observer that ran local builds on the same laptop (`INCIDENT_OBSERVER_CONTENTION.md`). It is not evidence the supervisor mechanism proved its worth.


## SK-H3 — the one substantive test, and it passes

> **Provenance.** Scored from the **mid-run insurance pass (hours 0–16)**, not the full run. The post-teardown capture lost its race: `cluster_backend` runs `eval-export` then `kind delete cluster`, and both had completed before the end-of-run watcher's 10 s poll noticed k6 had exited. Hours 17–24 of hour-bucket data — the OOM period — are unrecoverable, because deleting the cluster destroys the Postgres holding them. The mid-run pass exists precisely because that race was judged losable in advance.

The hour buckets SK-H3 requires are not produced by the harness at all: `eval-export` emits run-level scalars and has no timeseries field, and `live_soak.yaml`'s `timeseries_reps: 0` leaves the DuckDB timeseries table empty. They were recovered from the per-event Postgres table by `eval/scripts/export_soak_buckets.sh`, mirroring the exporter's own method — CRUD is empty `model_tier`, and the percentile is nearest-rank via `percentile_disc`, since `percentile_cont` interpolates and disagrees in the fourth decimal.


Server-side over the captured window: **17,000,266 CRUD events**, crud_p95 **2.2275 ms**, crud_p99 **8.0089 ms** (B2's committed p99 is 8.0072 ms), mean_violation **1.76e-07**.


| hour (UTC) | events | crud_p95 ms | crud_p99 ms | violation | vs 8.0072 |
|---|---:|---:|---:|---:|---|
| 2026-08-18 09:00:00 | 151,765 | 3.6192 | 8.0371 | 0.00e+00 | ok |
| 2026-08-18 10:00:00 | 1,075,201 | 3.0371 | 8.0218 | 0.00e+00 | ok |
| 2026-08-18 11:00:00 | 1,075,200 | 2.2299 | 8.0091 | 0.00e+00 | ok |
| 2026-08-18 12:00:00 | 1,075,200 | 2.0394 | 8.0075 | 0.00e+00 | ok |
| 2026-08-18 13:00:00 | 1,075,200 | 2.1083 | 8.0084 | 0.00e+00 | ok |
| 2026-08-18 14:00:00 | 1,071,431 | 2.9557 | 8.0117 | 0.00e+00 | ok |
| 2026-08-18 15:00:00 | 1,075,201 | 2.0378 | 8.0077 | 0.00e+00 | ok |
| 2026-08-18 16:00:00 | 1,075,200 | 2.0349 | 8.0075 | 0.00e+00 | ok |
| 2026-08-18 17:00:00 | 1,075,200 | 2.0551 | 8.0080 | 0.00e+00 | ok |
| 2026-08-18 18:00:00 | 1,072,860 | 3.6099 | 8.0319 | 0.00e+00 | ok |
| 2026-08-18 19:00:00 | 1,058,733 | 2.4073 | 8.0097 | 0.00e+00 | ok |
| 2026-08-18 20:00:00 | 1,072,623 | 2.7308 | 8.0105 | 0.00e+00 | ok |
| 2026-08-18 21:00:00 | 1,075,200 | 2.0859 | 8.0081 | 0.00e+00 | ok |
| 2026-08-18 22:00:00 | 1,075,200 | 2.0616 | 8.0079 | 0.00e+00 | ok |
| 2026-08-18 23:00:00 | 1,075,200 | 2.0682 | 8.0080 | 0.00e+00 | ok |
| 2026-08-19 00:00:00 | 1,075,200 | 2.0756 | 8.0081 | 0.00e+00 | ok |
| 2026-08-19 01:00:00 | 710,831 | 2.1984 | 8.0086 | 4.22e-06 | ok |

## Faults — all eight fired

Parsed from the committed injector logs. Their wall-clock strings are **UTC** (local is UTC+6), and each injector numbers its own faults from 1, so `T+offset` is the only unambiguous key.


| # | T+ | fault | UTC |
|---:|---:|---|---|
| 1 | 2 h | `planner-crash` | 11:50:27 |
| 2 | 5 h | `apiserver-throttle` | 14:50:27 |
| 3 | 8 h | `planner-crash` | 17:50:27 |
| 4 | 11 h | `apiserver-throttle` | 20:50:27 |
| 5 | 14 h | `planner-crash` | 23:50:27 |
| 6 | 17 h | `apiserver-throttle` | 02:50:27 |
| 7 | 20 h | `planner-crash` | 05:50:27 |
| 8 | 23 h | `apiserver-throttle` | 08:50:27 |

**8/8** frozen offsets fired exactly as scheduled.


### The planner-crash faults never interrupted planning

Registered in `MIDRUN_FINDINGS.md` **before** fault 7 fired, with both readings interpreted in advance. The prediction was that the planner's endpoint gap is shorter than the 10 s plan interval, which would explain five planner kills producing zero fallback lines.


Measured gap: **26 s**, one contiguous span — 2.6x the plan interval. Fallback lines produced: **0**. The registered prediction is falsified, and the alternative reading applies as registered.

The probe's own state column carries the likely mechanism: the pod reported **1/1 ready throughout termination**. Kubernetes delists a deleting pod from `Endpoints` at once but does not close established connections, and the operator reaches the planner over a reused HTTP connection — so it kept talking to the dying pod and saw no outage. Recorded as inference; confirming it needs a connection-level test that could not run against a live soak.


## Disposition — the retry loop stops here

`PREREG_LIVE_SOAK_V2.md` §Outcome handling fixed this response before the run: *"A third failure would make 'the local harness cannot sustain a 24 h live run on this machine' itself the finding worth reporting, rather than something to keep retrying past."* This is the third SK-H4 failure. The loop stops.

That framing is sharpened by what this attempt established. It is not that the machine is inadequate — the control plane served 25.6M requests at a 2.2 ms server-side p95 while being asked to. It is that **`kubectl port-forward` cannot carry 300 req/s for 24 h**, and that pinning the entire workload to one of sixteen replicas drives that pod into an OOM cycle. That is specific and fixable, where "the laptop isn't good enough" would not have been.


What a fifth sitting would need, before it would be worth running:

- **A load path that is not a port-forward** — NodePort, Ingress, or an in-cluster generator — so traffic reaches all sixteen replicas and no single pod carries the workload.
- **A violation metric with dynamic range.** A 375 ms target against an 8 ms p99 makes SK-H1 unfailable; the target has to sit near the observed distribution.
- **An audit sink, or no audit hypothesis.** SK-H2 cannot be scored while `POLYFORGE_NATS_URL` is unset everywhere.
- **A planner fault that actually stops the planner** — `--grace-period=0` or scaling to zero — since deletion alone leaves it serving established connections.
- **Bucketed metrics from `eval-export` directly**, so a run does not depend on rescuing Postgres before teardown.
- **No local builds or heavy queries for the duration**, a protocol rule this run did not have (`INCIDENT_OBSERVER_CONTENTION.md`).

The 24 h duration itself is not in doubt: the run reached its target and the fault schedule executed in full. What is in doubt is whether anything was measured, and for four of five hypotheses the answer is no.

