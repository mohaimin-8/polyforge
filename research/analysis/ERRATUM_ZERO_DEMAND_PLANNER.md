# Erratum: Phase 7 and the live-chaos sitting ran with a planner that read zero demand

Generated by `erratum_zero_demand_planner.py` from the committed run tables (`eval/results/phase7_live_runs.csv`, `eval/results/live_chaos_p99_runs.csv`). Descriptive: no hypothesis, no verdict. The verdict records stand as committed.

## Mechanism

Commit `33bd83c` (2026-08-08 11:26 UTC) records the defect and fixes it: the live demand window (`RPSWindow`) was never populated by any production emitter, so the live planner read zero demand for every tenant and the controller held its initial configuration for whole runs. Every live record committed before that fix measured the joint controller's INITIAL configuration, not its plans.

## Which live records predate the fix

| record | first committed | commit | before the fix? |
|---|---|---|---|
| `PHASE7_ORDINAL.md` | 2026-07-16 | `6c68766` | **yes** |
| `RESULTS_LIVE_CHAOS_P99.md` | 2026-07-19 | `56c329f` | **yes** |
| `RESULTS_LIVE_SOAK_V8.md` | 2026-08-30 | `8b28e7b` | no |
| `RESULTS_TRACE_LIVE.md` | 2026-09-01 | `929df84` | no |
| `RESULTS_MULTINODE.md` | 2026-09-05 | `fc47133` | no |
| `RESULTS_WAVE4_LIVE_PLANE.md` | 2026-08-30 | `7f0a392` | no |
| `RESULTS_WAVE4_CALIBRATED.md` | 2026-09-16 | `880f6f6` | no |

## Phase 7 (`PHASE7_ORDINAL.md`)

The run table dates every run between 2026-07-15 12:45 and 2026-07-15 17:35 UTC (laptop local time, UTC+6): all before the fix. **10 of 12** runs are valid, not 12 of 12:

| arm | cell | valid / runs |
|---|---|---|
| `hpa` | `ai_cacheable` | 3 / 3 |
| `hpa` | `crud_bursty` | 3 / 3 |
| `jcac` | `ai_cacheable` | 3 / 3 |
| `jcac` | `crud_bursty` | 1 / 3 |

So the Phase 7 `jcac` arm held its initial configuration for the whole of every run. Its ordinal comparison is "JCAC's starting configuration against real HPA", and the DISAGREE verdict, which stands as recorded, is not evidence about the controller's plans. The `crud_bursty` cell rests on one valid `jcac` run.

## Live chaos (`RESULTS_LIVE_CHAOS_P99.md`, LC-H1)

The sitting predates the fix, so the "last-known-good plan" the record credits with absorbing the planner crash and the API-server throttle was the initial configuration: the planner had never made a plan. The recorded `mean_violation` is 0, 0 in the 2 cells, against a target the data plane stays far below. As scored, LC-H1 could not have failed. Its PASS stands as recorded and shows that the data plane served through the faults, not that a planner fallback carried a plan.

## What this changes

* Any claim that Phase 7 shows "the full loop runs live — features to planner to operator" is not supported by these runs.
* LC-H1 is a demonstration that the data plane survives the faults, not a test of the controller's fallback.
* The live records from 2026-08-30 onward (soak V8, trace live, multinode, B1, B1′, B1″) post-date the fix and are not affected by this defect.

