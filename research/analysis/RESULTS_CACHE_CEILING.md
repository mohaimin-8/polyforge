# The semantic cache's precision ceiling — retrieval is NOT the binding constraint (C1)

Generated by `analysis_cache_ceiling.py`. Every number is computed at run time from the committed LMSYS-Chat-1M parquets.

**`CACHE_PRECISION.md` is unchanged and stands as committed.** This record tests one interpretation that record *declares* — that its low response agreement is model stochasticity rather than retrieval error — and reports the answer whichever way it lands.


## The oracle

Restricted to prompts whose text is **exactly duplicated** in the corpus. Retrieval there is perfect by construction: a cache would serve a response stored against a character-identical prompt, so the embedder cannot be at fault for any disagreement that remains. The **same-model** stratum additionally removes the cross-model style confounder that `CACHE_PRECISION.md` names, and is therefore the cleanest estimate.

Sample 120,000 (prompt, response) pairs, seed 42; 107,586 distinct prompts, **4,008 with duplicates**; at most 20 pairs scored per group so a single large group cannot become the estimate.


**C1 PASS** — 10,441 duplicate pairs over 4,008 groups (floor: 1,000 pairs, 100 groups).


## C2 — measured

| stratum | pairs | mean agreement | median | precision @0.60 | **@0.70** | @0.80 |
|---|---:|---:|---:|---:|---:|---:|
| all duplicate pairs | 10,441 | 0.5075 | 0.5302 | 0.4295 | **0.3324** | 0.2233 |
| **same-model** (cleanest) | 3,193 | 0.6089 | 0.6891 | 0.5888 | **0.4898** | 0.3724 |
| cross-model | 7,248 | 0.4628 | 0.4677 | 0.3593 | **0.2631** | 0.1576 |


## C3 — the verdict

| quantity | value |
|---|---:|
| published overall precision (tau=0.85, rho=0.70) | 0.313 |
| published same-model stratum | 0.443 |
| **oracle same-model precision** | **0.4898** |
| rule fixed in advance | > 0.60 means retrieval binds |

**Retrieval is NOT the binding constraint.** Even with perfect retrieval the cache serves a disagreeing response 51.0% of the time — against 55.7% at the measured operating point, so removing all retrieval error recovers only +0.047. **No embedder swap can raise the headline**, and `CACHE_PRECISION.md`'s declared reading is confirmed by measurement rather than asserted.

The practical consequence for the thesis: the cache's precision limit is a property of the *workload* — the same question answered differently — not of PolyForge's retrieval. That is why only relative cache readings are claimed, and it is now evidenced.

