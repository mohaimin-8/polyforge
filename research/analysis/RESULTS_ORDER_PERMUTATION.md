# Sweep-order permutation (V-series)

Generated by `analysis_order_permutation.py`. Pre-registration: `PREREG_ORDER_PERMUTATION.md`, committed before the campaign ran.

**No published result is replaced.** The published arm is `jcac` with `sorted()` order; the permutation arms are the identical controller under five fixed alternative orders (R1/R4).


## OP-H1 — is the published order inside the permutation range?

| mix | published Jain | perm min | perm max | published is |
|---|---:|---:|---:|---|
| besteffort_heavy | 0.9372 | 0.9296 | 0.9371 | **above the range** (by 0.0001) |
| premium_heavy | 0.9221 | 0.9221 | 0.9274 | below the range (by 0.0000) — *least* favorable |
| whale | 0.9490 | 0.9448 | 0.9503 | inside the range |

**OP-H1 FAIL** — the published order falls outside the permutation range on besteffort_heavy, premium_heavy, so the strict test does not hold.


**But the direction matters, and it is not systematic.** The published order is the most favorable on besteffort_heavy and the *least* favorable on premium_heavy. The largest excursion is **0.0001** in Jain — an order of magnitude below the 0.01 materiality threshold this prereg fixed in advance for OP-H2.


## OP-H2 — does order matter more where priority is heterogeneous?

| mix | Jain spread across the 6 orders | > 0.01 |
|---|---:|---|
| besteffort_heavy | 0.0076 | no |
| premium_heavy | 0.0053 | no |
| whale | 0.0055 | no |

**OP-H2 FAIL** — `whale` spread 0.0055 (threshold 0.01).


## OP-H3 — is cost order-dependent too?

- Cost spread across the six orders: **0.91%** of the mean (threshold 5%)

**OP-H3 PASS** — cost is essentially order-independent, so the cost comparison does not inherit this confound.


## Per-order results (all cells pooled)

| arm | mean_jain | total_cost_usd | mean_violation | mean_excess |
|---|---:|---:|---:|---:|
| `jcac` *(published, sorted)* | 0.9361 | 2.6165 | 0.1197 | 0.4255 |
| `jcac_perm1` | 0.9348 | 2.6146 | 0.1200 | 0.4905 |
| `jcac_perm2` | 0.9350 | 2.6109 | 0.1199 | 0.4564 |
| `jcac_perm3` | 0.9353 | 2.6150 | 0.1208 | 0.4317 |
| `jcac_perm4` | 0.9375 | 2.6011 | 0.1175 | 0.4559 |
| `jcac_perm5` | 0.9331 | 2.6249 | 0.1219 | 0.4697 |

## What this means

**The confound is real but immaterial, and this is the honest reading.** The audit was right that the sweep order is a free parameter that was never varied and that it correlates with priority class by construction. But measured, the effect is **below the 0.01 materiality threshold this pre-registration fixed in advance, on every mix including the `whale` fairness stressor** (OP-H2 FAIL), and the published order is not systematically favorable — it is the best order on one mix and the *worst* on another. Cost is order-independent to within 0.91% (OP-H3 PASS).


So the published fairness results stand. What changes is that the sweep order is now an explicit, seeded controller parameter (`tenant_order_seed`) with a measured spread on record, rather than an unexamined consequence of `sorted()` — so a reviewer who asks the obvious question ('what if you rename t00 to t99?') has a pre-registered answer with 540 runs behind it instead of an argument.


Strictly, OP-H1 fails: the published mean lies 0.0001 outside the permutation range on 2 of 3 mixes. That is reported as a FAIL rather than reasoned away — but a Jain excursion of 0.0001 is not a finding a fairness claim turns on, and the prereg fixed the threshold that says so before the numbers existed.

