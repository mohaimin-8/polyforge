% PolyForge — journal manuscript scaffold (FGCS / elsarticle).
%
% STRUCTURE ONLY: every stub section below is a TODO(author) block pointing
% at the source-of-truth document its prose should be carved from. The two
% completed sections (sec-eviction, sec-jcac) are \input as-is.
%
% Build (from research/paper/):
%   pdflatex main && bibtex main && pdflatex main && pdflatex main
%
% Figures: \graphicspath resolves the committed evaluation figures
% (eval/results/figures, regenerable via scripts/reproduce.py) and the
% local figures/ dir (jcac-pareto.png is a copy of
% research/results/jcac/pareto.png, regenerated by research/jcac_sim/sweep.py).
% Bibliography: single source of truth is thesis/report/references.bib.
%
% Venue notes: FGCS submits with elsarticle (preprint,review). For IEEE
% TCC/TSC, swap the class for IEEEtran and \bibliographystyle{IEEEtran};
% the section files are class-agnostic.
\documentclass[preprint,review,12pt]{elsarticle}

\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{algorithm}
\usepackage{algpseudocode}

\graphicspath{{figures/}{../../eval/results/figures/}{../results/jcac/}}

\journal{Future Generation Computer Systems}

\begin{document}

\begin{frontmatter}

% TODO(author): confirm the working title.
\title{PolyForge: Joint Cross-Layer Control of Replicas, Semantic Cache,
and Model Tier for Multi-Tenant Hybrid AI Serving}

% TODO(author): fill in author, affiliation, and email.
\author[inst1]{AUTHOR}
\affiliation[inst1]{organization={INSTITUTION}, city={CITY}, country={COUNTRY}}

\begin{abstract}
% TODO(author): ~200 words. Sources: RESULTS_MASTER.md scoreboard for the
% headline numbers (-70% BurstGPT / -42% Azure cost at violation parity,
% Jain win vs tuned VTC-replica, Pareto-undominated in the 1,800-run
% matrix); ARCHITECTURE.md for the one-sentence system description; the
% central claim is the joint-control thesis (per-layer local optima do not
% compose).
\end{abstract}

\begin{keyword}
% TODO(author): pick 5-7. Candidates: multi-tenant serving \sep
% LLM inference \sep autoscaling \sep semantic caching \sep model routing
% \sep SLO management \sep cloud cost optimization
\end{keyword}

\end{frontmatter}

\section{Introduction}
% TODO(author): carve from thesis/report introduction chapter +
% docs/RELATED_WORK.md §4 (the "what the 2026 stack does not do" paragraph
% is the gap statement). Contributions list = the six scoreboard segments
% in research/analysis/RESULTS_MASTER.md.

\section{Related work}
% TODO(author): carve from docs/RELATED_WORK.md (verified sources §5:
% SageServe, Chiron, Aladdin, VTC/Equinox/D2LPM, GPTCache/SCALM/InstCache,
% llm-d, AIBrix, Dynamo) — the capability table in its §1 maps directly to
% a related-work table here.

\section{Workload taxonomy and problem setting}
\label{sec:taxonomy}
% TODO(author): carve from research/TAXONOMY.md (formal 5-class taxonomy,
% 12-feature window vectors) + the classifier description in
% ARCHITECTURE.md. The model-tier price table sec-eviction cites as
% Table~\ref{tab:inference-bench} belongs here — source:
% docs/INFERENCE_BENCH.md (give it \label{tab:inference-bench}).

\input{sec-eviction}

\input{sec-jcac}

\section{System architecture and implementation}
\label{sec:implementation}
% TODO(author): carve from ARCHITECTURE.md (the loop end-to-end, the
% component table, ADR highlights 0003/0008/0014/0015) — control plane,
% AI gateway, classifier, planner service (imports the simulator's
% controller: offline and online planning are the same code), operator
% with guardrails.

\section{Evaluation}
\label{sec:evaluation}
% TODO(author): carve from research/analysis/RESULTS_MASTER.md (scoreboard
% + campaign ledger). Every number is regenerable: scripts/reproduce.py
% re-derives the records byte-identically from the committed
% eval/results/metrics_*.csv.gz exports.
% Figure inventory with captions: eval/results/figures/FIGURES.md.
% Suggested pulls: fig03 (Pareto), fig08 (ablations), fig12 (composite J),
% fig13 (forecast ablation), fig14 (realism Pareto); real-trace results
% from RESULTS_TRACE2.md (BurstGPT) and RESULTS_TRACE_AZURE.md; the
% 2026-stack baseline from RESULTS_CONCURRENCY.md; tenant scale from
% RESULTS_TENANT_SCALE.md; live evidence from RESULTS_LIVE_CHAOS_P99.md
% (and the B1 live plane once executed).

\subsection{Real-trace replays}
\label{sec:traces}
% TODO(author): carve from RESULTS_TRACE2.md (BurstGPT, 10.63M requests)
% and RESULTS_TRACE_AZURE.md (Azure LLM 2024, 44.1M requests) — this is
% the "deciding experiment" sec-eviction defers to.

\section{Threats to validity and limitations}
% TODO(author): carve from the thesis discussion/limitations chapter and
% the honest-reporting record: v2 H1 FAIL (SLO iso-attainment framing),
% fairness gamma-null, seasonal non-transfer (FORECAST_TRACE_REAL.md),
% TS-H1b direction-consistent FAIL, the Wave-5 clamp disclosure
% (RESULTS_MOVE_CLAMP.md), sim-substrate scope + the PHASE7 live ordinal
% cell (DEFENSE_QA carries the adjudications).

\section{Conclusion}
% TODO(author): one page; mirror the introduction's contribution list
% against the measured verdicts.

% Data and code availability statement (FGCS asks for one):
% TODO(author): repository URL + Zenodo DOI + OSF registration DOIs once
% minted (docs/RELEASE_CHECKLIST.md items).

\bibliographystyle{elsarticle-num}
\bibliography{../../thesis/report/references}

\end{document}
